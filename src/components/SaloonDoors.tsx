// Lightweight wrapper to keep compatibility with any imports that reference
// `src/components/SaloonDoors`. The real implementation lives in
// `SaloonDoorsFixed.tsx` (kept to avoid destructive edits during iteration).
export { default } from './SaloonDoorsFixed';
